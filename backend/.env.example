# Base de données PostgreSQL
DB_NAME=blablabook
DB_USER=student
DB_PASSWORD=blablabook2025
DB_HOST=localhost
DB_PORT=5432

# Sécurité JWT
# IMPORTANT: Générez un secret aléatoire sécurisé pour la production
# Vous pouvez utiliser: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=votre_secret_jwt_super_securise_a_changer

# Configuration serveur
PORT=3000
NODE_ENV=development

# CORS - URL du frontend
FRONTEND_URL=http://localhost:5173
